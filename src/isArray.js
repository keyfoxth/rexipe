/* @flow */

/**
 * Check if `value` is an `array`.
 *
 * @export
 * @param {any} value The value to check.
 * @return {boolean} Return `true` if `value` is an `array`, else `false`.
 */
export default Array.isArray
